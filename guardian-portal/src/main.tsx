import React,{useEffect,useMemo,useRef,useState}from'react';import{createRoot}from'react-dom/client';import{io}from'socket.io-client';import'./styles.css';
type Location={latitude:number;longitude:number;lastUpdatedAt:number|null;connectionState:'CONNECTED'|'DISCONNECTED';freshness:'FRESH'|'STALE'};type State={riderName:string;rideStatus:'live'|'ended';startedAt:string;location?:Location;separationState:'unknown'|'separated'|'reunited';observerCredential?:string};const api=import.meta.env.VITE_API_BASE_URL||window.location.origin;let googleLoader:Promise<void>|null=null;
function loadMaps(key:string){if(window.google?.maps)return Promise.resolve();if(googleLoader)return googleLoader;googleLoader=new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=`https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(key)}&v=weekly`;s.async=true;s.onload=()=>window.google?.maps?resolve():reject();s.onerror=reject;document.head.append(s)});return googleLoader}
function Map({location}:{location?:Location}){const host=useRef<HTMLDivElement>(null),map=useRef<google.maps.Map|null>(null),marker=useRef<google.maps.Marker|null>(null),[status,setStatus]=useState<'LOADING'|'READY'|'FAILED'|'MISSING_KEY'>(()=>import.meta.env.VITE_GOOGLE_MAPS_API_KEY?'LOADING':'MISSING_KEY');useEffect(()=>{const key=import.meta.env.VITE_GOOGLE_MAPS_API_KEY;if(key)loadMaps(key).then(()=>setStatus('READY')).catch(()=>setStatus('FAILED'))},[]);useEffect(()=>{if(status!=='READY'||!host.current||!location)return;const p={lat:location.latitude,lng:location.longitude};if(!map.current){map.current=new google.maps.Map(host.current,{center:p,zoom:16,zoomControl:true,streetViewControl:false,mapTypeControl:false,fullscreenControl:false,gestureHandling:'greedy'});marker.current=new google.maps.Marker({map:map.current,position:p,title:'Rider location'})}else marker.current?.setPosition(p)},[status,location?.latitude,location?.longitude]);if(!location)return <div className="map-placeholder">Location not available yet.</div>;if(status==='MISSING_KEY')return <div className="map-placeholder">Map temporarily unavailable — Google Maps key is not configured.</div>;if(status==='FAILED')return <div className="map-placeholder">Map temporarily unavailable.</div>;return <div className="map-wrap"><div ref={host} className="google-map"/>{status==='LOADING'&&<div className="map-loading">Loading map…</div>}</div>}
function App(){const[state,setState]=useState<State|null>(null),[error,setError]=useState<string|null>(null),[events,setEvents]=useState<string[]>([]),token=useMemo(()=>window.location.hash.slice(1),[]);useEffect(()=>{if(!token){setError('This Guardian Portal link is unavailable.');return}let off=false;fetch(`${api}/api/guardian-portal/bootstrap`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}).then(r=>r.ok?r.json():Promise.reject()).then((d:State)=>{if(!off){setState(d);history.replaceState(null,'',window.location.pathname)}}).catch(()=>!off&&setError('This Guardian Portal link is unavailable.'));return()=>{off=true}},[token]);useEffect(()=>{if(!state?.observerCredential||state.rideStatus!=='live')return;const s=io(`${api}/guardian-portal`,{auth:{credential:state.observerCredential},transports:['websocket']});s.on('portal:location',(p:{latitude:number;longitude:number;lastUpdatedAt:number})=>setState(x=>x?{...x,location:{...p,connectionState:'CONNECTED',freshness:'FRESH'}}:x));s.on('portal:presence',(p:Location)=>setState(x=>x?{...x,location:x.location?{...x.location,...p}:x.location}:x));s.on('portal:separated',()=>{setState(x=>x?{...x,separationState:'separated'}:x);setEvents(x=>['Rider separated from the group',...x])});s.on('portal:reunited',()=>{setState(x=>x?{...x,separationState:'reunited'}:x);setEvents(x=>['Rider reunited with the group',...x])});s.on('portal:sos',()=>setEvents(x=>['Emergency alert: rider triggered SOS',...x]));s.on('portal:rideEnded',()=>setState(x=>x?{...x,rideStatus:'ended',observerCredential:undefined}:x));s.on('portal:revoked',()=>setError('This Guardian Portal link is no longer available.'));return()=>{s.close()}},[state?.observerCredential,state?.rideStatus]);if(error)return <main><h1>Guardian Portal</h1><p>{error}</p></main>;if(!state)return <main><h1>Guardian Portal</h1><p>Loading live ride…</p></main>;const stale=!state.location||state.location.freshness==='STALE';return <main><header><strong>GUARDIAN ANGEL</strong><span>Guardian Portal</span></header><h1>{state.riderName}</h1><p className={state.rideStatus==='ended'?'ended':stale?'stale':'live'}>{state.rideStatus==='ended'?'Ride ended':stale?'Temporarily offline — showing last known location':'Live ride'}</p><p>Ride started {new Date(state.startedAt).toLocaleString()}</p><Map location={state.location}/><p>Last updated: {state.location?.lastUpdatedAt?new Date(state.location.lastUpdatedAt).toLocaleTimeString():'—'}</p>{state.separationState==='separated'&&<p className="sos">Separation alert active</p>}<section><h2>Safety updates</h2>{events.length?events.map((e,i)=><p key={i}>{e}</p>):<p>No safety alerts.</p>}</section></main>};createRoot(document.getElementById('root')!).render(<App/>);

